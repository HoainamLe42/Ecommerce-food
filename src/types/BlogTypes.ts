export type BlogTypes = {
    id: number;
    time: string;
    readMin: string;
    title: string;
    desc: string;
    name: string;
    img: string;
    author_avatar: string;
};

export type BlogFilterTypes = {
    id: string;
    label: string;
};
